import React from 'react';
import '../../styles/components/layout/Header.css'

const Header = (props) => {
    return (
        <header>
            <img src="images/cafe.png" alt="Empresa" />
            <h1>Empresa</h1>
        </header>
    );
}

export default Header;